\name{gtfs_ptal}
\alias{gtfs_ptal}
\docType{data}
\title{
Calculate EDF using GTFS data
}
\description{
This function calculates the Equivalent Doorstep Frequency (EDF) using GTFS data. It produces two types of output: the aggregated EDF for each point, which can then be used to determine the overall accessibility level, and the EDF value by mode for each point.
}
\usage{
gtfs_ptal(sarea_center, gtfs, stop_times_sum, stop_route, road_net, gtfs_mode, walk_kph=4.8, bus_dist=640, rail_dist=960, ferry_dist=960)
}
\format{
%%  ~~ If necessary, more details than the __description__ above ~~
}
\arguments{
  \item{sarea_center}{The location points to be analysed. These can be provided as grid points, selected POIs (points of interest), or centroid of census zones or mesh blocks.}
  \item{gtfs}{A list of GTFS data, containing all the required elements (i.e., \code{stops}, \code{stop_times}, \code{routes}, \code{calendar}, \code{calendar_dates}, and \code{trips}). This can be generated using function \code{read_gtfs()}.}
  \item{stop_times_sum}{The trips within the specified time interval. This can be generated by function \code{gtfs_summary()}.}
  \item{stop_route}{The corresponding service route of each stop. This can be generated by function \code{gtfs_summary()}.}
  \item{road_net}{The road network dataset. This can be generated by function \code{OSM_Network()}.}
  \item{gtfs_mode}{A user-defined table specifying the classification of transport modes. The table must include three fields: \code{route_type}, \code{mode_type}, and \code{mode}.\cr \code{route_type} corresponds to the field in the GTFS \code{routes.txt} file. \code{mode_type} follows the definitions in the \href{https://developers.google.com/transit/gtfs/reference/extended-route-types?hl=en}{Google Extended GTFS Route Types} specification and related GTFS documentation, such as the fileset consumer guide provided by \href{https://opendata.transport.nsw.gov.au/sites/default/files/2024-10/TfNSW_Realtime_Bus_Technical_Doc_v4.4.pdf}{Transport for New South Wales, Australia}. \code{mode} allows three values: \code{bus}, \code{rail}, and \code{ferry}.}
  \item{walk_kph}{The walking speed (kilometre per hour). The default value is set to 4.8 kph suggested in \href{https://content.tfl.gov.uk/connectivity-assessment-guide.pdf}{PTAL Guidebook} published by Transport for London.}
  \item{bus_dist}{The maximum walking distance to the bus services. The default value is set to 640 metres suggested in \href{https://content.tfl.gov.uk/connectivity-assessment-guide.pdf}{PTAL Guidebook} published by Transport for London..}
  \item{rail_dist}{The maximum walking distance to the rail services. The default value is set to 960 metres suggested in \href{https://content.tfl.gov.uk/connectivity-assessment-guide.pdf}{PTAL Guidebook} published by Transport for London.}
  \item{ferry_dist}{The maximum walking distance to the ferry services. The default value is set to 960 metres.}
}
\author{
Maintainer: Chia-Jung (Robert), Yeh  \email{chia-jung.yeh@sydney.edu.au}
}
\details{
%%  ~~ If necessary, more details than the __description__ above ~~
}
\source{
%%  ~~ reference to a publication or URL from which the data were obtained ~~
}
\references{
%%  ~~ possibly secondary sources and usages ~~
}
\examples{
gtfs_ptal(sarea_center, gtfs, stop_times_sum, stop_route, road_net, gtfs_mode, walk_kph=4, bus_dist=400, rail_dist=800, ferry_dist=800)
}
\keyword{PTAL}

